{
  "name": "founderos",
  "version": "1.0.0",
  "private": true,
  "description": "FounderOS - AI-powered multi-agent startup copilot (LangGraph + Bun + Hono)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "build": "bun run --cwd backend build && bun run --cwd frontend build",
    "db:up": "docker compose up -d db",
    "db:migrate": "bun run --cwd backend migrate",
    "db:seed": "bun run --cwd backend seed",
    "stack:up": "docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
